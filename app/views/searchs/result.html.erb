<%= content_tag :h3, "Showing #{@videos.videos.size.to_s} results for #{@title}" %>

<% @videos.videos.each do |v| %>
 <%= v.title %> <br>
 <%= image_tag(v.thumbnails.first.url) %> <br>
 <%#= v.methods %>
<% end %>